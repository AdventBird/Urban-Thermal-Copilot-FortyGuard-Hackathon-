# Urban Thermal Copilot -- FortyGuard Hackathon '26 (Track 4)
# Python 3.11+ (developed/run on 3.14).
#
# Heavy geo deps (geopandas pulls shapely/pyogrio) are imported lazily so the
# pure-logic modules still import in a bare environment.
pandas>=2.0
numpy>=1.24
geopandas>=1.0
shapely>=2.0
pyproj>=3.4
folium>=0.16
branca>=0.6          # pulled in by folium, listed explicitly for the colormap
streamlit>=1.32
streamlit-folium>=0.20
plotly>=5.0
python-dotenv>=1.0
requests>=2.31
google-generativeai>=0.7    # AI executive report (Gemini); optional fallback if absent
matplotlib>=3.7       # optional: Feature 8 PNG export
pytest>=8.0